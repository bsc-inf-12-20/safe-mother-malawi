import 'package:flutter_test/flutter_test.dart';

void main() {
  // Placeholder — widget tests require Hive and platform channel setup.
  // Run the app with `flutter run` to verify UI behaviour.
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
